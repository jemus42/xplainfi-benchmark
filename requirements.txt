# Python package requirements for xplainfi-benchmark
# Using minimum versions (>=) for compatibility across Python 3.11+
# uv will resolve compatible versions automatically

numpy>=1.26.0        # Minimum version compatible with Python 3.12+
pandas>=2.1.0        # Minimum version compatible with Python 3.12+
scikit-learn>=1.3.0  # Stable version with Python 3.12+ support
sage-importance>=0.0.4

# fippy from specific commit for reproducibility
# Note: Using upstream version; our fork with RFI .to_numpy() fix is available at:
# git+https://github.com/jemus42/fippy@fix-rfi-to-numpy-bug
git+https://github.com/gcskoenig/fippy@a7a37aa5511f7074ead3289c89b1ae80036982cb
